{
  "pipeline": "gru_only",
  "notes": {
    "data": "Run python -m src.train to refresh processed CSVs and feature column pickles.",
    "model": "Train and export GRU via notebooks/deep_learning_model_selection.ipynb -> gru_attn_best.pkl"
  }
}
